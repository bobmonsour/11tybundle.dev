---
permalink: /showcase/
---
<header class="main-header">
  <div class="container">
    <h1>Showcase <data class="count" value="{{ showcasedata.siteCount }}">{{ showcasedata.siteCount | singleComma }}</data></h1>
    <nav>
      <ul role="list">
        <li><a href="/showcase" class="current">Sites</a></li>
        <li><a href="/sitesfeed.xml">RSS</a></li>
      </ul>
    </nav>
  </div>
</header>
<div class="section" data-pagefind-ignore="all">
  <div class="showcase__grid">
    {% for site in showcasedata.sites %}
      {% if loop.index <= 50 %}
        <figure class="showcase__card showcase--category">
          <img
            src="{{ site.screenshotpath }}"
            height="1080"
            sizes="(min-width: 1920px) 1920px, 100vw"
            alt="Screenshot of {{ site.title | safe }}"
            eleventy:widths="640,1366,1920"
            >
          <figcaption>
            {{ site.favicon | genFaviconHtml(1) | safe }}
            <a href="{{ site.link | getHostname | slugify }}">{{ site.link }}</a>
              <div class="details visually-hidden">
                {% if site.description %}
                    <p>{{ site.description | safe }}</p>
                {% else %}
                    <p>(no description)</p>
                {% endif %}
              </div>
          </figcaption>
        </figure>
      {% endif %}
    {% endfor %}
  </div>
</div>
