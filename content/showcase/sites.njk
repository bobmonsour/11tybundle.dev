---js
{
	eleventyComputed: {
		title: "{{ site.title }}",
		description: "{{ site.description | safe }}",
		permalink: "/showcase/{{ site.link | getHostname | slugify }}/",
    ogImage: "{{ site.screenshotpath }}",
    ogImageAlt: "Screenshot of {{ site.link | safe }}",
	},
	pagination:	{
		data: "showcasedata.sites",
		size: 1,
		alias: "site",
		addAllPagesToCollections: "true",
	}
}
---
<header class="main-header" data-pagefind-body>
  <div class="container">
    {{ site.favicon | genFaviconHtml(1) | safe }}
    <h1 data-pagefind-ignore>{{ site.title | safe }}</h1>
    <p class="visually-hidden" data-pagefind-meta="title" data-pagefind-weight={{ pfweights.showcaseDetail }}>Showcase: {{ site.title | upper | safe }}</p>
    <p class="visually-hidden" data-pagefind-meta="description">{{ site.description | safe }}</p>


      <div class="details" data-pagefind-ignore="all">
        {% if site.description %}
          <p>{{ site.description | truncate(140) | safe }}</p>
        {% else %}
          <p>&nbsp;</p>
        {% endif %}
      </div>

    {% if site.leaderboardLink %}
      <nav>
        <svg width="24" height="24" viewBox="0 0 24 24" aria-hidden="true">
          <use xlink:href="#icon-eleventy"></use>
        </svg>
        <a href="{{ site.leaderboardLink }}">Leaderboard Link</a>
      </nav>
    {% endif %}

  </div>
</header>

<div class="section">
  <div class="container flow">
    <a href="/showcase" class="button">
      <svg width="20" height="20" viewBox="0 0 24 24" aria-hidden="true">
        <use xlink:href="#icon-arrow-left"></use>
      </svg> Back to the showcase
    </a>

    <figure class="showcase__card">
      <img
        src="{{ site.screenshotpath }}"
        height="1080"
        sizes="100vw"
        alt="Screenshot of {{ site.title | safe }}"
        eleventy:widths="640,1280,1920"
        >
      <figcaption>
        <a href="{{ site.link }}">{{ site.link }}</a>
      </figcaption>
    </figure>

    <nav>
      {%- if pagination.href.next or pagination.href.previous %}
        <ol role="list" class="pagination">
          {%- if pagination.href.previous %}<li><a href="{{ pagination.href.previous }}" class="button"><svg width="20" height="20"
              viewBox="0 0 24 24" aria-hidden="true" eleventy:ignore>
              <use xlink:href="#icon-arrow-left"></use>
            </svg> Previous</a></li>
          {% else %}
            <li>&nbsp;&nbsp;</li>
          {% endif %}
          {%- if pagination.href.next %}<li><a href="{{ pagination.href.next }}" class="button"> Next<svg width="20" height="20"
              viewBox="0 0 24 24" aria-hidden="true" eleventy:ignore>
              <use xlink:href="#icon-arrow-right"></use>
            </svg></a></li>{% endif %}
        </ol>
      {%- endif %}
    </nav>

  </div>
</div>
