<!DOCTYPE html>
<html lang="en">
  {% include 'partials/head.njk' %}
<body>
	<a href="#main-content" id="visually-hidden">Skip to main content</a>
  <header>
    <p class="brand-name"><a href="/" class="brand-link">{% raw %}{{ 11tybundle.dev }}{% endraw %}</a></p>
    {% include "partials/sitenav.njk" %}
  </header>
  <main id="main-content">
    {# provide per-page styling via frontmatter #}
    {% if pageId %}
      <div id="{{ pageId }}">
        {{ content | safe }}
      </div>
    {% else %}
      {{ content | safe }}
    {% endif %}
  </main>
  <footer>
    <p class="rssfeedlink"><em>Don't miss an issue. Grab the <a href="https://www.11tybundle.dev/feed.xml">RSS feed</a>.</em></p>
    {% include "partials/sitenav.njk" %}
  </footer>
</body>
</html>
