{#
  The header of the page and the footer include different navigation.
  Those includes are in the default layout file. Styling can be accomplished
  through the selection of the <header> and <footer> elements in CSS.
#}
<nav class="brand">
  <ul class="brand-nav">
    {% for link in site.mainNavLinks %}
      <li>
        <a href="{{ link.url }}" class="nav-link"
          {% if link.url in page.url and link.url !== "/" %} aria-current="page"{% endif %}>{{ link.text }}</a>
      </li>
    {% endfor %}
  </ul>
</nav>
