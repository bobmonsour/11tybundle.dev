---
eleventyComputed:
  title: "{{ bundledata.categoryCount }} Categories"
  description: A list of {{ bundledata.categoryCount }} post categories
permalink: /categories/
keywords: categories, 11ty Bundle, 11ty, eleventy
image:
  source: "11tybundle-dev.jpg"
  alt: "an address plate for house number 11"
---

<h1>{{ title }}</h1>
<div class="allcategories-list">
  <ul class="categorieslist">
    {% for category in bundledata.categories %}
      {% set categoryUrl %}/categories/{{ category[0] | slugify }}/{% endset %}
      <li><a href="{{categoryUrl}}" ping="http://localhost:8888/.netlify/functions/countclicks?itemtype=categories&itemid={{ category[0] | urlencode }}">{{ category[0] }} ({{ category[1] }})</a></li
    {% endfor %}
  </ul>
</div>
