---
eleventyComputed:
  title: "{{ bundledata.categoryCount }} Categories"
  description: A list of {{ bundledata.categoryCount }} post categories
permalink: /categories/
---

<h1>{{ title }} <span class="reduced-to-md">(<a href="/categories-by-count/">by count</a>)&nbsp;(<a href="/categories-test/">new design</a>)</span></h1>
<div class="allcategories-list">
  <ul class="categorieslist">
    {% for category in bundledata.categories %}
      {% set categoryUrl %}/categories/{{ category[0] | cachedSlugify }}/{% endset %}
      <li><a href="{{categoryUrl}}">{{ category[0] }} <span class="reduced-to-base">({{ category[1] }})</span></a></li>
    {% endfor %}
  </ul>
</div>
