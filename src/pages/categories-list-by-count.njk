---
eleventyComputed:
  title: "{{ bundledata.categoryCount }} Categories"
  description: "A list of ({{ bundledata.categoryCount }}) of the post categories"
permalink: /categories-by-count/
---

<h1>{{ title }} <span class="reduced-to-md">(<a href="/categories-by-count/">by count</a>)</span></h1>
<div class="allcategories-list">
	<div class="categorieslist">
		{% for category in bundledata.categoriesByCount %}
			{% set categoryUrl %}/categories/{{ category[0] | slugify }}/{% endset %}
			<div class="onecategory">
				<a href="{{categoryUrl}}" class="onecattitle">{{ category[0] }} <span class="reduced-to-base">({{ category[1] }})</span></a>
				<ul class="recentcatitems">
					{% for item in bundledata.bundleRecords | postsInCategory(category[0], 3) %}
						<li><a href="{{ item.Link }}" data-link-type="external">{{ item.Title }}</a></li>
					{% endfor %}
				</ul>
			</div>
		{% endfor %}
	</div>
</div>
